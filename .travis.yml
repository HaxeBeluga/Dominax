language: java

before_script:
  - sudo add-apt-repository ppa:eyecreate/haxe -y
  - sudo apt-get update
  - sudo apt-get install haxe -y
  - mkdir ~/haxelib
  - haxelib setup ~/haxelib
  - haxelib git "git://github.com/HaxeBeluga/Beluga.git"

script:
  - haxe BelugaDemo.hxml